# จุดต่อของงาน — ปรับระบบให้พร้อมใช้งาน

## รอบ 21 กันยายน 2569: ตัวช่วยเตรียมใช้งานจริง
- เพิ่ม Setup-Sarabun-Production.cmd + PowerShell รับรหัสผ่านซ่อนอักขระ/ยืนยันสองครั้ง ส่งผ่าน stdin ไป Node ไม่ใส่รหัสใน command line
- configure-production.mjs ตรวจรูปแบบก่อนเขียน .env.local แบบไฟล์ชั่วคราวแล้ว rename รักษา SESSION_SECRET/Google config/DATABASE_PATH เดิม ไม่เปลี่ยนคีย์ที่ใช้เข้ารหัส Drive
- เพิ่มแผงความพร้อมใช้งานจริงในหน้าตั้งค่า แสดงเฉพาะสถานะและขั้นตอน ไม่ส่ง password/secret กลับ browser
- ทดสอบ helper 2 กรณีผ่าน ครอบคลุมอักขระพิเศษ รหัสผิดไม่เขียนไฟล์ และรักษาคีย์; typecheck, PowerShell syntax และ browser readiness ผ่าน
- Production build ผ่านแล้ว
- ยังไม่ได้ตั้งรหัสผ่านจริงให้ผู้ใช้ และ OAuth Client ยังไม่ถูกนำเข้า ต้องใช้ข้อมูลจากเจ้าของระบบ ไม่ใช่ปัญหาลิมิต
- เซิร์ฟเวอร์ dev เริ่มทำงานที่ 127.0.0.1:3000 อีกครั้ง; งานถัดไปหลังตั้งค่าคือทดสอบ Drive จริงและสิทธิ์หลายผู้ใช้ตามแผน

## ตัวค้นหางาน 20 กันยายน 2569
- เพิ่ม “อยากทำอะไร?” บนหัวเว็บ และ Ctrl/Cmd+K พร้อมค้นจากประโยคภาษาไทยและคำใกล้เคียงใน catalog ของฟังก์ชัน
- จัดลำดับทางลัด แสดงเหตุผลที่เกี่ยวข้อง รองรับ Enter/ลูกศร/Escape และมือถือ; ไม่เรียก LLM/บริการภายนอก
- เลือกแบบหนังสือแล้วเปิดร่างได้ทันที เมนูอื่นเปิดหน้าของงานนั้น ไม่มีการบันทึก/อัปโหลดอัตโนมัติจากคำค้น
- รักษาคำเตือนงานที่ยังไม่ได้บันทึกก่อนออกจาก editor/โอนงบ
- Unit search 2 ผ่าน, browser search 2 ผ่าน รวมยกเลิกการออกจากร่างและจอมือถือ; typecheck/build ผ่าน
- ขอบเขต: ค้นทางลัดเมนูและแบบหนังสือ ไม่ได้ค้นเนื้อหาเอกสารทุกฉบับ และไม่ใช่ AI แบบ LLM

## รอบพัฒนาต่อ 20 กันยายน 2569
- เพิ่มหน้ากู้คืนใน StorageSettings: แสดงชุดสำรองปัจจุบัน กู้สำเนาลงโฟลเดอร์ใหม่ และคัดลอกพาธได้; ตรวจ checksum/integrity ผ่านเครื่องมือเดียวกับ CLI
- จำกัดการเลือกชุดสำรองให้อยู่ในโฟลเดอร์ที่ตั้งค่า ตรวจพาธจริง ปฏิเสธ traversal และไม่เปลี่ยนฐานข้อมูลใช้งาน
- เพิ่ม Open-Sarabun-Production.cmd + production preflight ตรวจ password/secret/build/APP_URL และตรวจโหมดเซิร์ฟเวอร์ผ่าน health ก่อนเปิด Chrome
- ทดสอบ unit ทั้งชุด 46 ผ่าน ก่อนเพิ่ม production preflight test อีก 1 ผ่านแยก; typecheck และ build ผ่าน
- ทดสอบเบราว์เซอร์หลัก 7 ผ่าน: backup UI, reference picker, budget 2, PDF preview, recovery 2
- production login smoke ด้วยฐานจำลองและพอร์ต 3109 ผ่าน: บังคับล็อกอิน, ปฏิเสธรหัสผิด, รับรหัสถูก แม้ LOCAL_DEMO=true; ปิดเซิร์ฟเวอร์ทดสอบแล้ว
- ยังไม่ครบแผน: ยังไม่มีการสลับฐานข้อมูลจริงจาก UI, ยังไม่ได้ตั้งรหัสผ่าน production บนเครื่องผู้ใช้, OAuth client ของแอปยังไม่มี, Drive จริง/หลายผู้ใช้/สายอนุมัติยังค้าง
- ส่งคำถามขอพาธไฟล์ OAuth ไปแล้ว รอข้อมูลที่จำเป็น ห้ามสมมติว่า Drive เชื่อมสำเร็จ
- ค่าลิมิตเริ่มรอบ: ใช้ 18% ใน 5 ชั่วโมง / 3% สัปดาห์ ไม่ใช่เหตุที่งาน OAuth ค้าง

## จุดตรวจ 20 กันยายน 2569 (ใหม่สุด)
- รัน unit tests ทั้งชุด: 45 ผ่าน; typecheck และ production build ผ่าน
- Browser recovery ผ่านทั้งโอนงบและหนังสือภายนอก หลังแก้ selector ให้เลือกแบบจากหัวข้อการ์ด
- แก้ข้อความผิดพลาดของรายการสำเนาที่เคยถูกซ่อนเมื่อไม่มีร่างกำลังแก้ ให้แสดง alert แยก
- เพิ่ม Open-Sarabun.cmd สำหรับเปิดระบบ dev บนเครื่องและ Chrome; ทดสอบกรณีเซิร์ฟเวอร์เปิดอยู่ผ่านแล้ว กรณีเริ่มจากเซิร์ฟเวอร์ปิดยังไม่ได้ตรวจรับ
- สำรอง/กู้คืนธุรกิจผ่านการทดสอบฐานจำลอง พร้อมตรวจ hash และไม่เขียนทับปลายทาง ยังไม่มี UI สลับฐานข้อมูลจริง
- GitHub origin: KTTNDev/Administater-system; รุ่นนี้เอา output ออกจากการติดตาม Git แต่เก็บไฟล์บนเครื่องไว้ ประวัติ commit เก่าที่มี output ยังอยู่ ไม่ได้ rewrite history
- งานค้าง: UI กู้คืนฐานข้อมูล, launcher สำหรับ production พร้อมตั้งค่าล็อกอิน, ทดสอบการทำงานจริงครบกระบวนการ, OAuth/Drive ในแอป และสิทธิ์หลายผู้ใช้
- ลิมิตตอนเริ่มรอบนี้: 5 ชั่วโมงใช้ 3%, สัปดาห์ใช้ 1%; ไม่ได้ติดลิมิตเดิมแล้ว

เปิดระบบ: ดับเบิลคลิก Open-Sarabun.cmd (ต้องมี Node และติดตั้ง dependencies/.env.local แล้ว) เป็นโหมด local development ไม่ใช่การรับรองพร้อมเปิดให้หลายเครื่องใช้

คำสั่งผู้ใช้ 16 กันยายน 2569: ทำตามลำดับให้เสร็จ และแจ้งชัดทุกครั้งที่หยุดว่าส่วนไหนสำเร็จ/ส่วนไหนค้าง ตรวจ usage เป็นช่วง ๆ ไม่ใช้ reset credit โดยไม่ได้รับอนุญาต

## สถานะเริ่มรอบนี้
- ลิมิตสัปดาห์ใช้ 96%, 5 ชั่วโมงใช้ 9%
- 1. สำเนากู้คืนอัตโนมัติหนังสือและโอนงบ: กำลังทำ ยังไม่ตรวจรับ
- 2. สำรอง/กู้คืนทั้งฐานข้อมูลและไฟล์แนบ: มีส่งออกเดิม ยังไม่มี restore ที่ทดสอบครบ
- 3. ตัวเปิดระบบสำหรับใช้งานประจำ: ยังไม่ทำ
- 4. ตรวจงานจริง ภายใน/ภายนอก/โอนงบ: มี automated tests เดิม ยังไม่ผ่านผู้ใช้ตรวจรับ
- 5. Drive: มีโค้ด แต่ไม่มี client ID/secret/token ในแอป ต้องจบ OAuth และทดสอบจริง
- 6. หลายผู้ใช้/สิทธิ์/สายอนุมัติ: ยังไม่ทำ

## จุดตรวจล่าสุด 16 กันยายน 2569
- ลิมิตล่าสุด: สัปดาห์ใช้ 100%, รอบ 5 ชั่วโมงใช้ 34%; tool ยังรายงาน ordinaryUsageAllowed=true แต่ไม่ควรถือว่างานทั้งหมดจบแล้ว
- ขั้น 1 เพิ่ม RecoveryPanel ในหนังสือและโอนงบ เก็บหลังหยุดพิมพ์ 1.5 วินาที ใน recovery_drafts; มีปุ่มเปิดสำเนา กู้เป็นรายการใหม่ ไม่เขียนทับทะเบียนเดิม โอนงบต้องยืนยันอำนาจใหม่
- TypeScript ผ่านหลังแก้โค้ดขั้น 1–2
- Unit tests recovery/storage รวม 5 ผ่าน: ป้องกันคำขอเก่าเขียนทับใหม่, แยกทะเบียน, กู้ฐานข้อมูล/ไฟล์แนบ, ปฏิเสธโฟลเดอร์ปลายทางที่มีอยู่, ปฏิเสธชุดสำรอง checksum ผิด
- Browser test โอนงบ: พิมพ์โดยไม่กดบันทึก → รอสำเนา → reload → กู้คืน ผ่าน
- Browser test หนังสือ: รอบแรกติด selector เมนูเก่า; แก้ให้เปิดเมนูแบบหนังสือก่อน อยู่ระหว่างตรวจซ้ำ ต้องอ่านผลก่อนระบุว่าผ่าน
- ขั้น 2 exportWorkspace เพิ่ม sarabun.sqlite ซึ่งประกอบใหม่เฉพาะตารางธุรกิจและไฟล์แนบ ไม่รวม settings/Google token พร้อม manifest schemaVersion 2; scripts/restore-backup.mjs กู้ลงโฟลเดอร์ใหม่เท่านั้น ผ่าน unit tests ยังไม่มี UI กู้คืน และยังไม่ได้ทดสอบสลับแอปจริงไปฐานที่กู้
- ขั้น 3–6 ยังไม่เสร็จ อย่าสรุปว่าใช้งาน production หรือเชื่อม Drive จริงสำเร็จ

ข้อจำกัดขั้น 1: เป็นสำเนากู้คืน ไม่ใช่บันทึกทะเบียนอัตโนมัติ; ต้องรอข้อความ “บันทึกสำเนากู้คืนแล้ว” ก่อนปิดหน้า, ข้อมูลผิด schema อาจไม่บันทึกและแจ้งเตือน, รายการแสดงล่าสุด 30 สำเนา ยังไม่มีปุ่มลบ/นโยบายอายุสำเนา

หลักการขั้นแรก: เก็บสำเนาระหว่างพิมพ์แยกจาก documents/budget_transfers ไม่ทำเครื่องหมายอนุมัติ ไม่เปลี่ยนฉบับที่ตรวจทานแล้ว กู้เป็นร่างใหม่เพื่อไม่แก้ทับข้อมูลจากหน้าต่างอื่น

ฐานข้อมูลจริง `data/sarabun.sqlite` ห้ามใช้เป็นฐานข้อมูลทดสอบทำลายข้อมูล ไฟล์ระเบียบอยู่ `data/budget-references` ต้องรวมในชุดสำรอง

เซิร์ฟเวอร์ใช้ scripts/run-next.mjs และ runtime mirror ตาม .runtime-local.json ถ้า dev ไม่รู้จัก route ใหม่ เคยแก้โดยหยุดเฉพาะ process ของโปรเจกต์ เปลี่ยนชื่อ `.next/dev` เก็บไว้ แล้วเริ่ม dev ใหม่ ห้ามลบข้อมูลผู้ใช้ และอย่าใช้ production แทนโดยพลการ เพราะ LOCAL_DEMO ทำงานเฉพาะ development
